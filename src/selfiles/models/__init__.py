"""Per-relay-model registries.

Three of them, from three different sources, and they drift -- which is why
`selfiles` ships a test that fails on a model present in one and missing from
another unless the asymmetry is written down.
"""
